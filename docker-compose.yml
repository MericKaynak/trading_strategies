services:
  trading-bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: trading-bot
    env_file:
      - path: .env
        required: false
    command: python -m src.main
