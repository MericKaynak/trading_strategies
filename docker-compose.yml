services:
  trading-bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: trading-bot
    env_file:
      - path: .env
        required: false
    command: uv run python -m src.main
